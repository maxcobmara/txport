<%= form_for(@vehicle_fine_type) do |f| %>
  <% if @vehicle_fine_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vehicle_fine_type.errors.count, "error") %> prohibited this vehicle_fine_type from being saved:</h2>

      <ul>
      <% @vehicle_fine_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
