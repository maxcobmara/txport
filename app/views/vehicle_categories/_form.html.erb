<%= form_for(@vehicle_category) do |f| %>
  <% if @vehicle_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vehicle_category.errors.count, "error") %> prohibited this vehicle_category from being saved:</h2>

      <ul>
      <% @vehicle_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
