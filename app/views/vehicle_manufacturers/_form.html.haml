= form_for @vehicle_manufacturer, remote: true do |f|
  = render 'shared/error_messages', object: f.object
  
  %td= f.text_field :name
  %td= country_select("vehicle_manufacturer", "country", [ "Malaysia", "Korea", "Germany" ])
  %td= f.submit (t "actions.create"), :class => 'btn btn-primary'